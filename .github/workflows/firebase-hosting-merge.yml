# This file was auto-generated by the Firebase CLI
# https://github.com/firebase/firebase-tools

name: Deploy to Firebase Hosting on merge
'on':
  push:
    branches:
      - master
jobs:
  build_and_deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - run: cd functions && npm ci && npm run build
      - uses: 'google-github-actions/deploy-cloud-functions@v1'
        with:
          name: 'app'
          runtime: 'nodejs16'
          service_account: '${{ secrets.FIREBASE_SERVICE_ACCOUNT_WOWTRADE }}'
          workload_identity_provider: projects/151006879681/locations/global/workloadIdentityPools/github-pool/providers/github-provider
      - uses: FirebaseExtended/action-hosting-deploy@v0
        with:
          repoToken: '${{ secrets.GITHUB_TOKEN }}'
          firebaseServiceAccount: '${{ secrets.FIREBASE_SERVICE_ACCOUNT_WOWTRADE }}'
          channelId: live
          projectId: wowtrade
